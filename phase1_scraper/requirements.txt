# Phase 1 — Data Ingestion Dependencies
# Install with: pip install -r requirements.txt

# ── Web Scraping ──────────────────────────────────────────────────────────────
playwright>=1.42.0            # Headless browser for JS-rendered pages
beautifulsoup4>=4.12.0        # HTML parsing helper
lxml>=5.1.0                   # Fast HTML/XML parser (used by BS4)

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.6.0               # Data models and validation

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load environment variables from .env
loguru>=0.7.2                 # Structured logging (simpler than stdlib logging)
